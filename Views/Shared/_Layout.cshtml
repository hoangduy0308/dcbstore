<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - DCBStore</title>
    
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" xintegrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa;
        }
        .navbar-brand {
            font-weight: 700;
            letter-spacing: .5px;
        }
        .nav-link {
            font-weight: 500;
        }
        .hero-section {
            background: linear-gradient(45deg, #007bff, #00d2ff);
            color: white;
            padding: 5rem 0;
            text-align: center;
        }
        .main-container {
            min-height: 70vh;
        }
        .site-footer {
            background-color: #343a40;
            color: #adb5bd;
            font-size: 0.9rem;
        }
        .site-footer a {
            color: #fff;
            text-decoration: none;
        }
        .site-footer a:hover {
            color: #0d6efd;
        }
        .dropdown-menu {
            border-radius: 0.75rem;
            box-shadow: 0 0.5rem 1rem rgba(0,0,0,.15)!important;
        }
        .nav-search {
            min-width: 300px;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
            <div class="container">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">DCBStore</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-lg-inline-flex justify-content-between">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Trang Chủ</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Products" asp-action="Index">Sản Phẩm</a>
                        </li>
                    </ul>
                    
                    <!-- === SỬA LỖI: Trỏ form tìm kiếm đến ProductsController === -->
                    <div class="mx-auto">
                         <form asp-controller="Products" asp-action="Index" method="get" class="nav-search">
                            <div class="input-group">
                                <input type="text" class="form-control" placeholder="Tìm kiếm sản phẩm..." name="SearchString" value="@ViewData["CurrentFilter"]">
                                <button class="btn btn-outline-primary" type="submit">
                                    <i class="fas fa-search"></i>
                                </button>
                            </div>
                        </form>
                    </div>
                    <!-- === KẾT THÚC THANH TÌM KIẾM MỚI === -->
                    
                    <div class="d-flex align-items-center">
                        <!-- Cart Icon -->
                        <a class="nav-link text-dark me-3" asp-controller="Cart" asp-action="Index" title="Giỏ hàng">
                            <i class="fas fa-shopping-cart fs-5"></i>
                            <span class="badge rounded-pill bg-danger" id="cart-item-count" style="position: relative; top: -10px; left: -5px; font-size: 0.6em;">0</span>
                        </a>

                        <!-- Account Dropdown -->
                        <div class="nav-item dropdown">
                            <a class="nav-link text-dark" href="#" id="accountDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" title="Tài khoản">
                                <i class="fas fa-user-circle fs-4"></i>
                            </a>
                            <div class="dropdown-menu dropdown-menu-end p-2" aria-labelledby="accountDropdown">
                                 <partial name="_LoginPartial" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    @if (ViewContext.RouteData.Values["controller"]?.ToString() == "Home" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" && string.IsNullOrEmpty(ViewData["CurrentFilter"] as string))
    {
        <section class="hero-section">
            <div class="container">
                <h1 class="display-4 fw-bold">Chào mừng đến với DCBStore</h1>
                <p class="lead my-4">Khám phá thế giới mua sắm trực tuyến với hàng ngàn sản phẩm chất lượng.</p>
                <a href="@Url.Action("Index", "Products")" class="btn btn-light btn-lg">Khám phá ngay</a>
            </div>
        </section>
    }

    <div class="container main-container mt-5">
        <main role="main" class="pb-5">
            @RenderBody()
        </main>
    </div>

    <footer class="site-footer py-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5>DCBStore</h5>
                    <p>Nền tảng thương mại điện tử uy tín, mang đến trải nghiệm mua sắm tuyệt vời cho mọi nhà.</p>
                </div>
                <div class="col-md-2 mb-3">
                    <h5>Sản phẩm</h5>
                    <ul class="list-unstyled">
                        <li><a href="#">Sản phẩm mới</a></li>
                        <li><a href="#">Khuyến mãi</a></li>
                        <li><a href="#">Bán chạy</a></li>
                    </ul>
                </div>
                <div class="col-md-3 mb-3">
                    <h5>Hỗ trợ khách hàng</h5>
                     <ul class="list-unstyled">
                        <li><a href="#">Trung tâm trợ giúp</a></li>
                        <li><a href="#">Chính sách đổi trả</a></li>
                        <li><a href="#">Hướng dẫn mua hàng</a></li>
                    </ul>
                </div>
                <div class="col-md-3 mb-3">
                    <h5>Kết nối với chúng tôi</h5>
                    <div>
                        <a href="#" class="me-3 fs-4"><i class="fab fa-facebook"></i></a>
                        <a href="#" class="me-3 fs-4"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="fs-4"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
            </div>
            <hr class="my-4" style="border-color: #495057;">
            <p class="text-center mb-0">&copy; @DateTime.Now.Year - DCBStore. All Rights Reserved.</p>
        </div>
    </footer>
    
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
